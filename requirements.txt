# Versions are pinned exactly (==), not as a minimum (>=), because reproducibility
# is a claimed contribution of this project (see paper, Section IV.D and README).
# In particular, scikit-image's SSIM API changed across versions -- the
# `multichannel` argument was removed in favour of `channel_axis` in 0.20 -- so a
# loose version range could silently change how SSIM is computed and undermine
# every recovery/imperceptibility number in the paper. Regenerate this file with
# `python -m pip freeze` immediately before any run whose numbers go into the paper.

numpy==2.1.2
opencv-python-headless==5.0.0.93
scikit-image==0.26.0
pillow==10.4.0
streamlit==1.61.1
matplotlib==3.11.1
python-pptx==1.0.2
